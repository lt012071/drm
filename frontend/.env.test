# Test environment configuration
# This file is used for unit tests

# API URL (not used when mock service is enabled)
VITE_API_URL=http://localhost:3001

# Mock service settings - enabled for unit tests
VITE_USE_MOCK_SERVICE=true

# Authentication settings - skipped for unit tests
VITE_SKIP_AUTH=true